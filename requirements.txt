# TALOS v5.9.17 -- Dependencies
# Install: pip install -r requirements.txt

# === Core: API Framework & Networking ===
fastapi                     # REST API framework (TALOS Facade -- main_api.py)
uvicorn[standard]           # ASGI server for FastAPI (port 8001)
pydantic                    # Data validation & serialization (Pydantic v2 API models)
httpx                       # Async HTTP client for external API calls
requests                    # HTTP requests (general, synchronous)
python-dotenv               # Environment variable management (.env)

# === MCP Protocol (v5.8.0+) ===
mcp                         # Model Context Protocol SDK

# === Core: AI & LLM Providers ===
google-generativeai         # Google Gemini API communication
google-genai                # Google Gemini API (new SDK -- Grey Literature)
openai                      # DeepSeek/OpenAI-compatible API communication

# === Sources: Academic APIs & Integrations ===
arxiv                       # ArXiv API client
semanticscholar             # Semantic Scholar API client
elsapy                      # Elsevier/Scopus API client
pymed                       # PubMed API client
pyzotero                    # Zotero Web API client

# === Data Science: Analysis & ML ===
numpy                       # Numerical operations & embedding calculations
pandas>=2.1.0,<3.0          # Data manipulation (DataFrames)
scikit-learn                # Machine learning (clustering, TF-IDF)
psutil                      # Process & system monitoring (daemon, VRAM queries)

# === Graphify AST Knowledge Graph (vendor/graphify) ===
tree-sitter                  # AST parsing library (required by vendored graphify)
tree-sitter-python           # Python language grammar for Graphify AST (v5.9.11 hotfix)
rapidfuzz                    # Fuzzy string matching for Graphify entity resolution (v5.9.11 hotfix)
# tree-sitter-language-pack  # Language grammars (install manually if needed)

# === Visualization & Scientometrics ===
plotly>=5.18.0              # Interactive 3D visualizations (GWO Swarm Hunt)
dash>=4.0.0                 # Live real-time GWO dashboard (Dash)
matplotlib>=3.8.0           # Basic plotting library
seaborn>=0.13.0             # Advanced statistical data visualization
wordcloud>=1.9.0            # Word cloud generation
networkx                    # Knowledge graph creation
pyvis                       # Interactive network visualization

# === Interface & UX ===
rich                        # Terminal UI beautification (Console, Panel, Table, Box)
questionary                 # Terminal UI (interactive menus)
tqdm                        # Progress bars

# === Reporting & Web Dashboard ===
Flask                       # Web server for interactive dashboard
Jinja2                      # Templating engine (HTML reports)
python-docx                 # Word document generation
tabulate                    # Markdown table formatting

# === Deep Reinforcement Learning (DRL) ===
gymnasium                   # RL environment standard (Gymnasium API)
torch                       # Neural network framework (PyTorch -- CPU or CUDA)

# === Testing (v5.8.0+) ===
pytest                      # Unit test framework

# === Web Search (Grey Literature Miner) ===
duckduckgo-search           # Web search for Grey Literature Miner